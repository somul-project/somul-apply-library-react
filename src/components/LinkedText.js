import React from 'react';


const LinkedText = ({children}) => {
    return (
        <div>
            <span>
                {children}
            </span>
        </div>
    );
};

export default LinkedText;